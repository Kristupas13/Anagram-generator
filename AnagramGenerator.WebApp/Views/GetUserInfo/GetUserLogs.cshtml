@model User

@foreach(var item in Model.UserLogs)
{
    <br />
    @item.Date
    <br />
    @item.SearchedWord
    <text>
        <font color="grey">
            has anagram 
        </font>
    </text>
    @item.Anagram
    <br />
       
}
